package com.cgi.pages;

import org.openqa.selenium.WebDriver;

/**
 * This class handles all the common elements on the employee management
 * application
 */
public class MainPage {
	WebDriver driver;

	public MainPage(WebDriver driver) {
		this.driver = driver;
	}
}
